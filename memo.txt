MPA (Multi Page Application) 의 단점

- 페이지 이동이 매끄럽지 않고 비 효율적임, 다수의 사용자 접속시, 서버의 부하가 심해 짐

npm i react-router-dom

// 1. "/" : 모든 일기를 조회하는 Home 페이지
// 2. "/new" : 새로운 일기를 작성하는 New 페이지
// 3. "/diary" : 일기를 상세히 조회하는 Diary 페이지

라우트 페이지 경로에서 없는 페이지 즉 404 같은 페이지는 경로를 path에 \* (와일드 카드) 경로로 지정
Routes 안에는 Route만

동적 경로 -> 동적인 데이터를 포함하고 있는 경로
- URL Parameter  ->  / 뒤에 아이템의 id를 명시
- Query String  ->  ? 뒤에 변수명과 값 명시  ( ~/search?q= 검색어 )

Query String 사용법은 useSearchParams 를 import 하고 useState처럼 사용
밑에는 예시
const [params, setParams] = useSearchParams();
console.log(params.get("value"))   

동적 페이지 이동은 useNavigate 사용으로 Link로 연결

### 이미지를 assets 경로에 넣은건 최적화를 위해서 (DATA URL로 캐싱되어 매번 불러오지 않음)
이미지가 엄청 많으면 경우가 또 다르니 그때는 public에 저장하는게 나음 소수의 이미지일때 assets
- public인 경우 -> <img src={"/emotion1.png"} />
- assets인 경우 -> import 로 불러온다음 <img src={emotion1} /> 

useState 보단 상태관리를 useReducer로 외부로 관리

초기 기존데이터 값이 있으므로 id 값을 활용하기 위해 useRef 사용

메인 home 페이지의 header 부분 먼저 UI 설정 그 다음 DiaryList 부분 Ui, 다음 DiaryItem UI 설정

home.jsx 에서 onIncreaseMonth, onDecreaseMonth 기능을 추가함으로 해더부분의 날짜 이동 기능 추가한다음
useContext 로 props 받아와서 getMonthlyData 함수인 해당달의 첫 시간과 끝 시간사이로 filter 해서 표시

DiaryList.jsx에서 toSorted 를 사용해서 최신순 오래된순을 정렬 

new 페이지는 우선 디자인에 따라서 먼저 UI부터 구현
Editor.jsx 에서 useState 를 객체로 만들어서 여러 데이터를 한번에 담음
input 에서 그냥 new Date()로 하면 value 값을 이해 못하므로 getStringedDate 함수를 만들어서
값을 넣어줌

Editor.jsx 에서 useContext 로 onCreate를 바로 사용안하는 이유는 해당 페이지 에서만 쓰는게아니라 
수정페이지등 다른곳에서도 공통으로 데이터를 쓰기때문에 props로 New.jsx에서 onCreate 사용

useNavigate에서 nav값안데 뒤에 인수로 replace속성은 뒤로가기 방지 역할

Edit.jsx에서 삭제하기 클릭시 경고문으로 window.confirm 사용

useDiary라는 useEffect 와 useState를 같이 사용한 함수를 여러곳에서 쓰는데 이걸 export 할 수가 없으니
따로 hooks폴더에 커스텀 훅으로 만들어서 사용 

getStringedDate라는 복잡한 날짜 형식의 함수도 공통으로 사용되어서 따로 util 폴더에 넣어서 사용